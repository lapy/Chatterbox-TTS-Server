# requirements-nvidia.txt — CUDA wheels via PyTorch cu124
# Resemble Chatterbox 0.1.7 expects torch 2.6.0. The official cu121 wheel index
# only ships torch up to 2.5.1, so we use the cu124 index (installs torch
# 2.6.0+cu124). Requires an NVIDIA driver new enough for CUDA 12.4+ runtime
# (typically 550+). RTX 20/30/40/Ada work with this stack.

# --- PyTorch CUDA 12.4 wheels (torch 2.6.0) ---
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# --- Core Application; chatterbox-tts is NOT listed here (see start.py) ---
# Manual: pip install -r requirements-nvidia.txt
#         pip install --no-deps git+https://github.com/resemble-ai/chatterbox.git@<chatterbox_pip.txt> s3tokenizer==0.3.0 onnx==1.16.0

# --- Chatterbox 0.1.7 explicit deps (resemble-ai/chatterbox pyproject) ---
conformer==0.3.2
diffusers==0.29.0
transformers==5.2.0
safetensors==0.5.3
librosa==0.11.0
pykakasi==2.3.0
gradio==6.8.0
pyloudnorm==0.1.1
spacy-pkuseg
omegaconf>=2.3.0
einops>=0.7.0
resemble-perth @ git+https://github.com/resemble-ai/Perth.git@master
resampy==0.4.3

# Core Web Framework
fastapi>=0.100.0,<0.116.0
uvicorn[standard]>=0.27.0

# ML & Audio Libraries
numpy>=1.24.0,<2.0.0
soundfile>=0.12.0
descript-audio-codec==1.0.0

# Configuration & Utilities
PyYAML>=6.0
python-multipart>=0.0.5
requests>=2.28.0
Jinja2>=3.1.2,<3.2.0
watchdog>=3.0.0
aiofiles>=23.0.0
unidecode>=1.3.0
inflect>=7.0.0
tqdm>=4.65.0
hf_transfer>=0.1.4

# Audio Post-processing
pydub>=0.25.0
audiotsm>=0.1.2
praat-parselmouth>=0.4.0
