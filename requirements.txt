# ============================================================================
# CHATTERBOX TTS SERVER - REQUIREMENTS (CPU-ONLY INSTALLATION)
# ============================================================================
# PyTorch stack matches resemble-ai/chatterbox 0.1.7 (torch 2.6.0+cpu).
# Chatterbox-tts is installed with --no-deps; packages below are the explicit
# dependencies from the upstream pyproject (plus server-only extras).

# --- PyTorch CPU-Only Installation Index ---
--extra-index-url https://download.pytorch.org/whl/cpu

# --- PyTorch 2.6.0 (Resemble Chatterbox 0.1.7) ---
torch==2.6.0+cpu
torchvision==0.21.0+cpu
torchaudio==2.6.0+cpu

# --- Core TTS Application ---
# IMPORTANT: chatterbox-tts is NOT included here. Install with --no-deps, then:
#   pip install --no-deps <chatterbox_pip.txt> s3tokenizer==0.3.0 onnx==1.16.0

# --- Chatterbox 0.1.7 explicit deps (resemble-ai/chatterbox pyproject) ---
conformer==0.3.2
diffusers==0.29.0
transformers==5.2.0
safetensors==0.5.3
librosa==0.11.0
pykakasi==2.3.0
gradio==6.8.0
pyloudnorm==0.1.1
spacy-pkuseg
omegaconf>=2.3.0
einops>=0.7.0
resemble-perth @ git+https://github.com/resemble-ai/Perth.git@master
resampy==0.4.3
# s3tokenizer / onnx installed alongside chatterbox via start.py (protobuf pins)

# --- Web Framework & Server (not in chatterbox; this repo) ---
fastapi>=0.100.0,<0.116.0       # starlette<1.0 needed for TemplateResponse compat
uvicorn[standard]>=0.27.0

# --- Audio / codec (server + TTS path) ---
numpy>=1.24.0,<2.0.0
descript-audio-codec==1.0.0
soundfile>=0.12.0
pydub>=0.25.0
audiotsm>=0.1.2
praat-parselmouth>=0.4.0

# --- Web & API Utilities ---
python-multipart>=0.0.5
requests>=2.28.0
Jinja2>=3.1.2,<3.2.0
aiofiles>=23.0.0
hf_transfer>=0.1.4

# --- Configuration & Data Processing ---
PyYAML>=6.0
watchdog>=3.0.0
unidecode>=1.3.0
inflect>=7.0.0
tqdm>=4.65.0
